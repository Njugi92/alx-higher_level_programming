#!/usr/bin/node
// script that print 3 lines using array & loop
const myArray = ['C is fun', 'Python is cool', 'Javascript is amaizing'];
for (let k = 0; k < myArray.length; k++) {
	console.log(myArray[k]);
}
